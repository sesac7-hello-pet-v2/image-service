server:
  port: 8088
spring:
  application:
    name: image-service
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 100MB
  profiles:
    active:
      - dev
